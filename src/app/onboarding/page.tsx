import { Logo } from "@/components/ui/logo";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { createWorkspace } from "./actions";

export default function OnboardingPage() { return <main className="grid min-h-screen place-items-center bg-muted p-5"><section className="surface w-full max-w-xl rounded-2xl p-7 sm:p-10"><Logo /><div className="mt-10"><p className="text-sm font-semibold text-primary">LET’S SET UP YOUR WORKSPACE</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Tell us about your business.</h1><p className="mt-2 text-sm text-muted-foreground">This information appears on your invoices. You can fine-tune it later in settings.</p></div><form action={createWorkspace} className="mt-8 space-y-5"><label className="block text-sm font-medium">Business name<Input required name="name" minLength={2} maxLength={120} className="mt-1.5" placeholder="Studio North" /></label><label className="block text-sm font-medium">Default currency<select name="currency" className="mt-1.5 h-10 w-full rounded-xl border bg-transparent px-3 text-sm"><option value="INR">Indian Rupee (INR)</option><option value="USD">US Dollar (USD)</option><option value="EUR">Euro (EUR)</option><option value="GBP">Pound Sterling (GBP)</option></select></label><Button size="lg" className="w-full">Create workspace</Button></form></section></main>; }
