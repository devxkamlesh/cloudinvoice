import { SettingsForm } from "@/components/settings/settings-form";
import { requireOrganization } from "@/lib/organization";
export default async function SettingsPage() { const organization = await requireOrganization(); return <main className="mx-auto max-w-3xl p-5 sm:p-7"><p className="text-sm font-medium text-muted-foreground">WORKSPACE</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Settings</h1><p className="mt-2 text-sm text-muted-foreground">Manage the information that makes each invoice yours.</p><div className="mt-7"><SettingsForm organization={organization} /></div></main>; }
