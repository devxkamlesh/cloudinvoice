import Link from "next/link";
import { InvoiceStatus } from "@prisma/client";
import { ArrowUpRight, FilePlus2, IndianRupee, UsersRound, WalletCards } from "lucide-react";
import { Button } from "@/components/ui/button";
import { formatDate, money } from "@/lib/utils";
import { prisma } from "@/lib/prisma";
import { requireOrganization } from "@/lib/organization";

export default async function DashboardPage() { const org = await requireOrganization(); const [invoices, clientCount] = await Promise.all([prisma.invoice.findMany({ where: { organizationId: org.id }, include: { client: { select: { name: true } } }, orderBy: { createdAt: "desc" }, take: 6 }), prisma.client.count({ where: { organizationId: org.id } })]); const paid = invoices.filter((invoice) => invoice.status === InvoiceStatus.PAID).reduce((total, invoice) => total + Number(invoice.total), 0); const outstanding = invoices.filter((invoice) => invoice.status === InvoiceStatus.SENT || invoice.status === InvoiceStatus.VIEWED || invoice.status === InvoiceStatus.PARTIALLY_PAID || invoice.status === InvoiceStatus.OVERDUE).reduce((total, invoice) => total + Number(invoice.total) - Number(invoice.amountPaid), 0); const cards = [{ label: "Paid revenue", value: money(paid, org.currency), hint: "From recent invoices", icon: IndianRupee }, { label: "Outstanding", value: money(outstanding, org.currency), hint: "Awaiting collection", icon: WalletCards }, { label: "Active clients", value: String(clientCount), hint: "In your workspace", icon: UsersRound }]; return <main className="mx-auto max-w-7xl p-5 sm:p-7"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-medium text-muted-foreground">Good to see you</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Your business at a glance</h1></div><Button asChild><Link href="/invoices/new"><FilePlus2 className="size-4" />New invoice</Link></Button></div><section className="mt-7 grid gap-4 md:grid-cols-3">{cards.map(({ label, value, hint, icon: Icon }) => <article key={label} className="surface rounded-2xl p-5"><div className="flex items-start justify-between"><p className="text-sm font-medium text-muted-foreground">{label}</p><Icon className="size-5 text-primary" /></div><p className="mt-5 text-2xl font-semibold tracking-tight">{value}</p><p className="mt-1 text-xs text-muted-foreground">{hint}</p></article>)}</section><section className="surface mt-7 overflow-hidden rounded-2xl"><div className="flex items-center justify-between px-5 py-5"><div><h2 className="font-semibold">Recent invoices</h2><p className="mt-1 text-sm text-muted-foreground">A pulse on your latest billing activity.</p></div><Link href="/invoices" className="inline-flex items-center gap-1 text-sm font-semibold text-primary">All invoices <ArrowUpRight className="size-4" /></Link></div>{invoices.length === 0 ? <div className="border-t px-5 py-14 text-center"><p className="font-medium">Your invoice list is ready when you are.</p><p className="mt-1 text-sm text-muted-foreground">Create an invoice to start tracking revenue.</p><Button className="mt-5" asChild><Link href="/invoices/new">Create invoice</Link></Button></div> : <div className="overflow-x-auto border-t"><table className="w-full min-w-[640px] text-left text-sm"><thead className="bg-muted/50 text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="px-5 py-3 font-medium">Invoice</th><th className="px-5 py-3 font-medium">Client</th><th className="px-5 py-3 font-medium">Due</th><th className="px-5 py-3 font-medium">Status</th><th className="px-5 py-3 text-right font-medium">Total</th></tr></thead><tbody>{invoices.map((invoice) => <tr key={invoice.id} className="border-t"><td className="px-5 py-4 font-medium"><Link className="hover:text-primary" href={`/invoices/${invoice.id}`}>{invoice.invoiceNumber}</Link></td><td className="px-5 py-4">{invoice.client.name}</td><td className="px-5 py-4 text-muted-foreground">{formatDate(invoice.dueDate)}</td><td className="px-5 py-4"><span className="rounded-full bg-muted px-2.5 py-1 text-xs font-medium capitalize">{invoice.status.replaceAll("_", " ").toLowerCase()}</span></td><td className="px-5 py-4 text-right font-medium">{money(invoice.total.toString(), invoice.currency)}</td></tr>)}</tbody></table></div>}</section></main>; }
