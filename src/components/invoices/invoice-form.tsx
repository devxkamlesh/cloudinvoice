"use client";
import { zodResolver } from "@hookform/resolvers/zod";
import { Plus, Trash2 } from "lucide-react";
import { useFieldArray, useForm } from "react-hook-form";
import { z } from "zod";
import { Button } from "@/components/ui/button";
import { Input, Textarea } from "@/components/ui/input";
import { invoiceSchema } from "@/lib/validations";
import { createInvoice } from "@/app/(dashboard)/invoices/actions";

type FormValues = z.input<typeof invoiceSchema>;
export function InvoiceForm({ clients, defaultTaxMode }: { clients: { id: string; name: string }[]; defaultTaxMode: "INTRA_STATE" | "INTER_STATE" }) { const form = useForm<FormValues>({ resolver: zodResolver(invoiceSchema), defaultValues: { clientId: clients[0]?.id ?? "", issueDate: new Date().toISOString().slice(0, 10) as unknown as Date, dueDate: new Date(Date.now() + 14 * 86400000).toISOString().slice(0, 10) as unknown as Date, taxMode: defaultTaxMode, template: "classic", items: [{ description: "", quantity: 1, unitPrice: 0, discount: 0, taxRate: 18 }], notes: "", terms: "Payment due within 14 days." } }); const { fields, append, remove } = useFieldArray({ control: form.control, name: "items" }); const items = form.watch("items"); const taxMode = form.watch("taxMode"); const calculated = items.reduce((sum, item) => { const base = Math.max(0, Number(item.quantity || 0) * Number(item.unitPrice || 0) - Number(item.discount || 0)); return { base: sum.base + base, tax: sum.tax + base * Number(item.taxRate || 0) / 100 }; }, { base: 0, tax: 0 }); async function submit(values: FormValues) { await createInvoice(values); } if (!clients.length) return <div className="surface rounded-2xl p-8 text-center"><p className="font-semibold">Add a client first</p><p className="mt-1 text-sm text-muted-foreground">An invoice needs a billing recipient.</p><Button asChild className="mt-5"><a href="/clients">Go to clients</a></Button></div>; return <form onSubmit={form.handleSubmit(submit)} className="grid gap-6 xl:grid-cols-[1fr_320px]"><div className="space-y-6"><section className="surface rounded-2xl p-5 sm:p-6"><h2 className="font-semibold">Invoice details</h2><div className="mt-5 grid gap-4 sm:grid-cols-3"><label className="text-sm font-medium sm:col-span-3">Bill to<select className="mt-1.5 h-10 w-full rounded-xl border bg-transparent px-3 text-sm" {...form.register("clientId")}><option value="">Select a client</option>{clients.map((client) => <option key={client.id} value={client.id}>{client.name}</option>)}</select></label><label className="text-sm font-medium">Issue date<Input type="date" className="mt-1.5" {...form.register("issueDate")} /></label><label className="text-sm font-medium">Due date<Input type="date" className="mt-1.5" {...form.register("dueDate")} /></label><label className="text-sm font-medium">Tax type<select className="mt-1.5 h-10 w-full rounded-xl border bg-transparent px-3 text-sm" {...form.register("taxMode")}><option value="INTRA_STATE">Intra-state (CGST + SGST)</option><option value="INTER_STATE">Inter-state (IGST)</option></select></label><label className="text-sm font-medium sm:col-span-3">Invoice template<select className="mt-1.5 h-10 w-full rounded-xl border bg-transparent px-3 text-sm" {...form.register("template")}><option value="classic">Classic — clear and traditional</option><option value="modern">Modern — calm and minimal</option><option value="midnight">Midnight — high-contrast statement</option></select></label></div>{form.formState.errors.clientId && <p className="mt-3 text-sm text-red-600">Select a client.</p>}{form.formState.errors.dueDate && <p className="mt-3 text-sm text-red-600">{form.formState.errors.dueDate.message}</p>}</section><section className="surface overflow-hidden rounded-2xl"><div className="flex items-center justify-between px-5 py-5 sm:px-6"><div><h2 className="font-semibold">Line items</h2><p className="mt-1 text-sm text-muted-foreground">Taxes are calculated per item.</p></div><Button type="button" variant="outline" size="sm" onClick={() => append({ description: "", quantity: 1, unitPrice: 0, discount: 0, taxRate: 18 })}><Plus className="size-3.5" />Add item</Button></div><div className="overflow-x-auto border-t"><div className="min-w-[780px] p-5 sm:p-6"><div className="grid grid-cols-[minmax(220px,1fr)_80px_110px_95px_75px_38px] gap-2 pb-2 text-xs font-medium uppercase tracking-wide text-muted-foreground"><span>Description</span><span>Qty</span><span>Rate</span><span>Discount</span><span>GST</span><span /></div>{fields.map((field, index) => <div key={field.id} className="grid grid-cols-[minmax(220px,1fr)_80px_110px_95px_75px_38px] gap-2 py-2"><Input aria-label="Item description" placeholder="Service or product" {...form.register(`items.${index}.description`)} /><Input aria-label="Quantity" type="number" min="0.01" step="0.01" {...form.register(`items.${index}.quantity`, { valueAsNumber: true })} /><Input aria-label="Unit price" type="number" min="0" step="0.01" {...form.register(`items.${index}.unitPrice`, { valueAsNumber: true })} /><Input aria-label="Discount" type="number" min="0" step="0.01" {...form.register(`items.${index}.discount`, { valueAsNumber: true })} /><Input aria-label="GST rate" type="number" min="0" max="100" step="0.01" {...form.register(`items.${index}.taxRate`, { valueAsNumber: true })} /><button type="button" disabled={fields.length === 1} onClick={() => remove(index)} className="grid place-items-center rounded-lg text-muted-foreground hover:bg-red-50 hover:text-red-600 disabled:opacity-30" aria-label="Remove item"><Trash2 className="size-4" /></button></div>)}{form.formState.errors.items && <p className="mt-2 text-sm text-red-600">{form.formState.errors.items.message}</p>}</div></div></section><section className="surface rounded-2xl p-5 sm:p-6"><h2 className="font-semibold">Notes and terms</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Notes<Textarea className="mt-1.5" placeholder="Thanks for your business." {...form.register("notes")} /></label><label className="text-sm font-medium">Payment terms<Textarea className="mt-1.5" {...form.register("terms")} /></label></div></section></div><aside className="surface h-fit rounded-2xl p-5 sm:p-6"><h2 className="font-semibold">Invoice total</h2><dl className="mt-5 space-y-3 text-sm"><div className="flex justify-between"><dt className="text-muted-foreground">Subtotal</dt><dd>₹{calculated.base.toFixed(2)}</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">{taxMode === "INTRA_STATE" ? "CGST + SGST" : "IGST"}</dt><dd>₹{calculated.tax.toFixed(2)}</dd></div><div className="border-t pt-4"><div className="flex justify-between text-base font-semibold"><dt>Total</dt><dd>₹{(calculated.base + calculated.tax).toFixed(2)}</dd></div></div></dl><Button type="submit" className="mt-6 w-full" disabled={form.formState.isSubmitting}>{form.formState.isSubmitting ? "Creating…" : "Create invoice"}</Button><p className="mt-3 text-center text-xs leading-5 text-muted-foreground">This starts as a draft. Review and email it when ready.</p></aside></form>; }
