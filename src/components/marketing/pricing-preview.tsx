"use client";

import Link from "next/link";
import { Check, Sparkles } from "lucide-react";
import { useState } from "react";
import { cn } from "@/lib/utils";

const plans = [
  { name: "Starter", monthly: "₹0", annual: "₹0", note: "For your first professional invoices", features: ["Core invoices", "GST calculations", "Client directory"], action: "Start free", href: "/sign-in" },
  { name: "Studio", monthly: "₹799", annual: "₹639", note: "For independent professionals building momentum", features: ["Everything in Starter", "Payment collection", "Revenue analytics"], action: "Start free", href: "/sign-in", featured: true },
  { name: "Business", monthly: "₹1,999", annual: "₹1,599", note: "For a more connected billing operation", features: ["Everything in Studio", "Advanced controls", "Priority support"], action: "See pricing", href: "/pricing" }
];

export function PricingPreview() {
  const [annual, setAnnual] = useState(false);
  return <section id="pricing" className="relative border-y border-white/[.08] bg-[#09090b] py-24 sm:py-32"><div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_0%,rgba(123,104,238,.18),transparent_32rem)]" /><div className="relative mx-auto max-w-7xl px-5"><div className="flex flex-col gap-6 sm:flex-row sm:items-end sm:justify-between"><div className="max-w-xl"><p className="marketing-eyebrow">PRICING THAT LEAVES ROOM TO GROW</p><h2 className="mt-4 text-3xl font-semibold tracking-[-.045em] text-white sm:text-5xl">Start with clarity. Upgrade when the work demands it.</h2><p className="mt-5 text-lg leading-8 text-zinc-400">No noisy pricing maze. Choose the level of control that fits your business today.</p></div><div className="inline-flex w-fit rounded-xl border border-white/[.1] bg-white/[.04] p-1"><button type="button" aria-pressed={!annual} onClick={() => setAnnual(false)} className={cn("rounded-lg px-3 py-2 text-sm font-semibold transition", !annual ? "bg-white text-zinc-950" : "text-zinc-400 hover:text-white")}>Monthly</button><button type="button" aria-pressed={annual} onClick={() => setAnnual(true)} className={cn("rounded-lg px-3 py-2 text-sm font-semibold transition", annual ? "bg-white text-zinc-950" : "text-zinc-400 hover:text-white")}>Yearly <span className="ml-1 text-[10px] text-emerald-500">SAVE 20%</span></button></div></div><div className="mt-12 grid gap-4 lg:grid-cols-3">{plans.map((plan) => <article key={plan.name} className={cn("relative rounded-2xl p-6", plan.featured ? "border border-violet-300/35 bg-gradient-to-b from-violet-400/[.16] to-white/[.04] shadow-[0_24px_80px_rgba(108,92,231,.13)]" : "marketing-card")}><div className="flex items-start justify-between"><div><h3 className="text-xl font-semibold text-white">{plan.name}</h3><p className="mt-2 min-h-10 text-sm leading-5 text-zinc-400">{plan.note}</p></div>{plan.featured && <span className="inline-flex items-center gap-1 rounded-full bg-violet-300/15 px-2.5 py-1 text-[11px] font-semibold text-violet-100"><Sparkles className="size-3" />Most popular</span>}</div><div className="mt-7"><span className="text-4xl font-semibold tracking-tight text-white">{annual ? plan.annual : plan.monthly}</span>{plan.monthly !== "₹0" && <span className="ml-1 text-sm text-zinc-500">/ month</span>}{annual && plan.monthly !== "₹0" && <p className="mt-2 text-xs text-emerald-400">Billed annually</p>}</div><Link href={plan.href} className={cn("mt-7 block rounded-xl px-4 py-3 text-center text-sm font-semibold transition", plan.featured ? "bg-white text-zinc-950 hover:bg-zinc-200" : "border border-white/10 bg-white/[.055] text-white hover:bg-white/10")}>{plan.action}</Link><ul className="mt-7 space-y-3 border-t border-white/[.08] pt-6">{plan.features.map((feature) => <li key={feature} className="flex items-center gap-2.5 text-sm text-zinc-300"><Check className="size-4 text-violet-300" />{feature}</li>)}</ul></article>)}</div><p className="mt-6 text-center text-xs text-zinc-500">Paid-plan checkout is not enabled yet. You can start with a free workspace today and explore launch pricing.</p></div></section>;
}
