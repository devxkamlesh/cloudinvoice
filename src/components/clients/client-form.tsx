"use client";
import { useState, useTransition } from "react";
import { Plus, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Input, Textarea } from "@/components/ui/input";
import { createClient } from "@/app/(dashboard)/clients/actions";

export function ClientForm() { const [open, setOpen] = useState(false); const [pending, startTransition] = useTransition(); const [error, setError] = useState<string>(); function action(data: FormData) { startTransition(async () => { const result = await createClient(data); if (result?.error) setError(result.error); else { setOpen(false); setError(undefined); } }); } return <><Button onClick={() => setOpen(true)}><Plus className="size-4" />New client</Button>{open && <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 grid place-items-center bg-slate-950/35 p-4"><form action={action} className="surface max-h-[90vh] w-full max-w-xl overflow-y-auto rounded-2xl p-6"><div className="flex items-start justify-between"><div><h2 className="text-lg font-semibold">Add a client</h2><p className="mt-1 text-sm text-muted-foreground">Their details can be reused on every invoice.</p></div><button type="button" onClick={() => setOpen(false)} className="rounded-lg p-1.5 hover:bg-muted" aria-label="Close"><X className="size-4" /></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium sm:col-span-2">Client name<Input name="name" required className="mt-1.5" /></label><label className="text-sm font-medium">Email<Input name="email" type="email" className="mt-1.5" /></label><label className="text-sm font-medium">Phone<Input name="phone" className="mt-1.5" /></label><label className="text-sm font-medium">GSTIN<Input name="gstin" className="mt-1.5" placeholder="27ABCDE1234F1Z5" /></label><label className="text-sm font-medium">State code<Input name="stateCode" className="mt-1.5" placeholder="27" maxLength={2} /></label><label className="text-sm font-medium sm:col-span-2">Billing address<Textarea name="billingAddress" className="mt-1.5" /></label><label className="text-sm font-medium sm:col-span-2">Internal notes<Textarea name="notes" className="mt-1.5" /></label></div>{error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-6 flex justify-end gap-3"><Button type="button" variant="outline" onClick={() => setOpen(false)}>Cancel</Button><Button disabled={pending}>{pending ? "Saving…" : "Save client"}</Button></div></form></div>}</>; }
